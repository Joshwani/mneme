services:
  mneme:
    build:
      context: ..
      dockerfile: Dockerfile
    environment:
      MNEME_DB: /data/mneme.db
    ports:
      - "8080:8080"
    volumes:
      - mneme_data:/data
      - ../examples:/examples:ro
    restart: unless-stopped

volumes:
  mneme_data:
