name: build-demo-index

on:
  workflow_dispatch:
  schedule:
    - cron: '21 3 * * 1'

jobs:
  demo-index-snapshot:
    name: Demo index snapshot
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: pyproject.toml
      - run: python -m pip install -e .
      - name: Build small demo index
        run: |
          mkdir -p data
          mneme --db data/mneme.db add-file examples/specs/todo.yaml
          # For a larger public demo index, uncomment this. The default limit is intentionally small.
          # mneme --db data/mneme.db ingest-apis-guru --limit 25
          mneme --db data/mneme.db stats
      - uses: actions/upload-artifact@v4
        with:
          name: mneme-demo-index
          path: data/mneme.db
